# Baseline: Settings/MessageTemplates/Index.vue

Зафиксировано: 2026-08-14, тенант studio2 (studio2.localhost:8000/settings/message-templates).

Скриншот: `screenshots/settings-message-templates.jpg`

## Структура
- «Настройки компании», вкладка «Шаблоны сообщений».
- Header-карточка с кнопкой «Добавить шаблон» — НЕТ `DataTableToolbar`, НЕТ чекбокс-колонки, `templates` — простой массив без пагинации (тот же паттерн, что Channels).
- Колонки — статичные.
- Строк на момент фиксации: 0 («Шаблонов ещё нет...»).

## Колонки
1. Название — полужирный
2. Триггер — синий бейдж (если задан) ИЛИ серый текст «Вручную» (если нет)
3. Текст — усечённый (`truncate max-w-md`) с `title` для полного текста
4. Статус — зелёный/серый бейдж
5. Действия — 2 иконки

## Консоль на момент фиксации
Без ошибок.

## На что смотреть после рефакторинга
- НЕТ `selectable`.
- Колонка «Триггер» — условный (бейдж/текст) через один слот.
- Колонка «Текст» — `truncate` + `title` атрибут не теряются.

## Результат после миграции на DataTable.vue (2026-08-14)
Сверено в браузере (LinuxChrome) после рефакторинга: визуально идентично baseline-скриншоту (пустое состояние), ни одной ошибки в консоли. Регресс не обнаружен.
