# Baseline: Central/Admin/Tenants/Index.vue

Зафиксировано: 2026-08-14, studio2/agent-sandbox/client1 в central БД (реальные тенанты платформы), хост `localhost:8000/admin/tenants`.

Скриншот: `screenshots/central-admin-tenants.jpg`

## Структура
- Пагинация: **нет** (`Tenant::get()`, весь список сразу).
- Тулбар: **нет** (ни поиска, ни фильтров, ни «Настроить столбцы»).
- Чекбокс-колонка: **нет**.
- Колонка «Действия»: **нет** (строки некликабельны, не ведут никуда).
- Строк на момент фиксации: **3** (studio2, tenant_agent_sandbox, client1).

## Колонки (порядок слева направо, все — простой текст)
1. Тенант (id, полужирный)
2. Домен
3. Создан (дата)
4. Пользователи (число)
5. Локации (число)
6. Клиенты (число)
7. Заказ-наряды (число)
8. Последняя активность (дата или «—»)

## Консоль браузера на момент фиксации
Только штатные Vite HMR debug-сообщения (`[vite] connecting...`/`[vite] connected.`), ни одного warning/error.

## На что смотреть после рефакторинга
- Все 8 колонок на месте, в том же порядке, с теми же заголовками.
- Числа/даты не съехали по строкам (studio2 = 1/4/4/2/дата, tenant_agent_sandbox = 0/681/1116/688/дата, client1 = 1/0/0/0/—).
- Строка `client1` с «—» в последней колонке (нет активности) отображается корректно, не ломает верстку.
- Ни одного нового предупреждения в консоли.

## Результат после миграции на DataTable.vue (2026-08-14)
Сверено в браузере (LinuxChrome) после рефакторинга: визуально идентично baseline-скриншоту (см. `screenshots/*-after.jpg`), ни одной новой ошибки/предупреждения в консоли, интерактивность (чекбоксы/select-all, клик по строке, кнопки действий, панель редактирования) проверена вручную и работает как раньше. Регресс не обнаружен.
